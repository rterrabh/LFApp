<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference"/>
        <attr name="metaButtonBarButtonStyle" format="reference"/>
    </declare-styleable>

    <declare-styleable name="AutomataView">

    </declare-styleable>

    <declare-styleable name="StateView">
        <attr name="name" format="string"/>
        <attr name="posX" format="float"/>
        <attr name="posY" format="float"/>
        <attr name="lineColor" format="color"/>
        <attr name="lineWidth" format="float"/>
        <attr name="internColor" format="color"/>
        <attr name="nameColor" format="color"/>
        <attr name="nameSize" format="float"/>
        <attr name="radius" format="float"/>
    </declare-styleable>

    <declare-styleable name="TransitionViewB">

    </declare-styleable>

</resources>
